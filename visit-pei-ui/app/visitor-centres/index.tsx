import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

const VisitorCentresScreen = () => {
  return (
    <ScreenPlaceholder
      title='Visitor Centres'
      description='This screen is ready for an alphabetical list of visitor centres and service information.'
    />
  );
};

export default VisitorCentresScreen;
